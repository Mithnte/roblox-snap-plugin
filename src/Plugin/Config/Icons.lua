-- Toolbar icon asset ids. Empty string = no icon (Studio shows text-only
-- buttons, which is safe/valid — unlike "rbxassetid://0", which is not a
-- real asset and can log warnings).
--
-- To add real icons: design/export square PNGs (see docs/icons.md), upload
-- each one via Studio's Asset Manager or create.roblox.com, then paste the
-- resulting "rbxassetid://<id>" here.
return {
	toggle = "",
	select = "",
	box = "",
	move = "",
	rotate = "",
	align = "",
	palette = "",
}
