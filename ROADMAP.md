# Roadmap (A–F focus)

### Short‑term
- Surface snap finalize + align‑to‑normal in Move
- Collision/overlap preview improvements — **done** (geometry‑accurate via `GetPartsInPart`)
- Per‑axis grid snap + axis-lock UI cues
- Linear / radial / grid array tools — **done**
- Align pack: center, match rotation/size — **done**

### Medium‑term (Premium)
- Vertex/edge/center snap — **done** (Move tool, toggle V, configurable threshold)
- Scale/resize tool — **done** (grid‑step resize, X/Y/Z constraint; Models scale uniformly)
- Multi‑axis rotate — **done** (Q/E around X/Y/Z)
- Paint (color/material) + match appearance — **done**
- Ghost placement improvements
- Palette/favorites + blueprints JSON — **done** (Blueprints panel: save/spawn/delete)
- Command palette + hotkey editor — **done** (rebindable keys in Hotkeys panel; Ctrl+P bound)
- Performance mode + safe mode

### Tooling
- CI: StyLua + selene + Luau compile check — **done**
- Pinned toolchain via Rokit — **done**
